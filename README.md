# Kafka Node.js Benchmark (Dockerized)

Benchmarks:

- KafkaJS
- node-rdkafka (librdkafka)
- @platformatic/kafka

## Requirements

- Docker
- Docker Compose

## Run

```bash
docker compose up --build
